# ansible_local runs each playbook on the VM itself, so the only host is localhost.
# Group membership (control_plane vs workers) is determined by which playbook Vagrant runs.

[control_plane]
localhost ansible_connection=local

[workers]
localhost ansible_connection=local
