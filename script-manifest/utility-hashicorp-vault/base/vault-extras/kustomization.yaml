apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vault

# Sync wave 0 — everything here must exist before the Vault StatefulSet starts,
# because the StatefulSet mounts the TLS Secret this Certificate produces.
resources:
  - certificate.yaml
  - reviewer-token.yaml
  - services.yaml
