apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied directly in Phase A0, not via the root app — cert-manager itself and this
# chain are prerequisites that must exist before ArgoCD creates Vault's Certificate.
resources:
  - issuers.yaml
